<resources>
    <!-- Vimed usa una única paleta clara (ver values/themes.xml).
         Este archivo existe para que el modo oscuro del sistema NO
         cambie el tema: hereda tal cual la versión clara. -->
    <style name="Theme.Vimed" parent="Base.Theme.Vimed" />
</resources>
