<?xml version="1.0" encoding="utf-8"?>
<!-- Tarjeta tintada de la familia violeta. El relleno de color es lo que
     saca a la app del blanco sobre blanco: el borde de la misma familia
     la sostiene sin necesidad de sombra. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/brand_50" />
    <corners android:radius="22dp" />
    <stroke android:width="1dp" android:color="@color/brand_100" />
</shape>
